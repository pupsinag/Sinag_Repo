; NPM configuration for production deployment
; This ensures dependencies are always installed fresh during deployment

# Treat missing peer dependencies as warnings, not errors
legacy-peer-deps=true

# Ensure production dependencies are always installed
production=false

# Use exact versions from package-lock.json
save=false
